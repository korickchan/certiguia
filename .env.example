SECRET_KEY=sua-chave-secreta-aqui
ADMIN_PASSWORD=admin123
CERTIFICADORA_PADRAO=certisign

# Catálogo de preços — varredura Playwright roda FORA do site (local ou CI):
#   python scripts/varredura_catalogo.py
# No Render deixe 0 (padrão). Só use 1 em dev local se quiser varrer ao subir o Flask.
CATALOGO_VARREDURA_INICIO=0
CATALOGO_VARREDURA_ONLINE=0

# Produção (Render, Cloud Run + Cloud SQL): URL PostgreSQL
# DATABASE_URL=postgresql://usuario:senha@host:5432/certiguia

PIX_CHAVE=99f89727-3fb9-4982-b54e-74dc097a078a
PIX_NOME=VICTOR NEVES
PIX_CIDADE=SALVADOR
